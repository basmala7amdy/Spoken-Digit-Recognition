# Spoken-Digit-Recognition
A simple speech recognition project that classifies spoken digits (0–9) using speech enhancement, MFCC feature extraction, and an SVM machine learning model. The system records audio, processes the speech signal, extracts features, and predicts the spoken digit.
